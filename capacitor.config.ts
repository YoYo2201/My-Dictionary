import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reign.dictionaryapp',
  appName: 'My Dictionary',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
